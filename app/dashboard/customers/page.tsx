import { Metadata } from "next";

export const metadata: Metadata = {
  title: 'Customers', // Template value for metadata in app\layout.tsx (see there)
};

export default function Page() {
  return <p>Customers Page</p>;
}
